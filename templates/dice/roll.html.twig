{% extends "base.html.twig" %}

{% block title %}Dice{% endblock %}

{% block body %}

<h1>{{ title }}</h1>

{{ include('./dice/navbar.html.twig') }}


<p>The dice is rolled {{ numRolls }} times.</p>

<ul>
    {% for roll in rolls %}
        <li>{{ roll }}</li>
    {% endfor %}
</ul>

{% endblock %}
